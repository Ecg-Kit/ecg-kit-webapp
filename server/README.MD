#Instal virtualenv
     > pip install virtualenv

     > virtualenv venv
        .....New python executable in venv/bin/python

     > pip install Flask

     > pip install flask-restful

     > . venv/bin/activate

To stop it
    > . venv/bin/deactivate